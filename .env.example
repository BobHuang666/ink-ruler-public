# 复制本文件为 .env 并填写实际值（不提交 .env 到版本库）
# 后端启动与种子脚本都会自动加载 .env

# 运行环境：development（默认） / production
# 生产环境必须设置 JWT_SECRET；开发环境未设 DB_NAME 时使用 ink-ruler-dev 库
# APP_ENV=development

# 数据库（可选，未设则用 config.yaml）
# 开发环境不设 DB_NAME 时自动用 ink-ruler-dev；生产在 Railway 等平台设置 DB_NAME
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
DB_PASSWORD=
# DB_NAME=ink-ruler

# JWT 密钥（生产环境必填，否则启动报错）
JWT_SECRET=
